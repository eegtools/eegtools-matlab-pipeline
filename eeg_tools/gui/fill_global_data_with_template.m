function fill_global_data_with_template(project)
%This function fills the GlobalData variable with the variables:
% - project (a struct ...)

global GlobalData
GlobalData.project = project;

end

